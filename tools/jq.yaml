version: '3'

tasks:
  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::scoop:install
        vars:
          APP: jq

  install:
    desc: "[JQ] ✨ Install jq"
    silent: true
    cmds:
      - task: install:windows
