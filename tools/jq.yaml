version: '3'

tasks:
  default:
    - task --list --taskfile tools/jq.yaml

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::scoop:install
        vars:
          APP: jq

  install:
    desc: "[JQ] ✨ Install jq"
    silent: true
    cmds:
      - task: install:windows
