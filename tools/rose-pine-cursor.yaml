version: '3'

includes:
  lib: ../lib.yaml
  tool: ../models/tool.yaml

tasks:
  default:
    - task: tool:default

  install:windows:
    platforms: [windows]
    internal: true
    cmds:
      - task: lib:scoop:neontowel-install
        vars:
          APP: "rose-pine-cursor"

  install:
    vars:
      APP_NAME: rose-pine-cursor
    desc: "[{{ .APP_NAME | upper }}] ✨ Install {{ .APP_NAME | title}}"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
