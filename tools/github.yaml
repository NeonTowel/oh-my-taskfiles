version: "3"

includes:
  github:
    taskfile: ../lib/github-v2.yaml
    flatten: true
    vars:
      HELP_PREFIX: "[GITHUB-CLI] ✨  "
      REPO: cli/cli
      DOWNLOAD_LINUX: 'gh_%s_linux_amd64.tar.gz'
      DOWNLOAD_DARWIN: 'gh_2.92.0_macOS_amd64.zip'
      DOWNLOAD_WINDOWS: 'gh_2.92.0_windows_amd64.zip'
      BINARY_RELEASE: true
      BINARY_LINUX: '**/gh'
      BINARY_DARWIN: '**/gh'
      BINARY_WINDOWS: '**/gh.exe'
      BINARY_PATH: '$HOME/bin'

tasks:
  default:
    - task --list --taskfile tools/github.yaml
