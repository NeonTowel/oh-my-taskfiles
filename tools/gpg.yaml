version: '3'

tasks:
  default:
    - task --list --taskfile tools/gpg.yaml

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::scoop:install
        vars:
          APP: gpg

  install:
    desc: "[GPG] ✨ Install GPG"
    silent: true
    cmds:
      - task: install:windows
