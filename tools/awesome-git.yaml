version: '3'

includes:
  lib: ../lib.yaml
  tool: ../models/tool.yaml

tasks:
  default:
    - task: tool:default

  install:
    desc: "[AWESOME-GIT] ✨ Install awesome-git"
    cmds:
      - task: install:windows
        platforms: [windows]
      - task: install:linux
        platforms: [linux]

  uninstall:
    desc: "[AWESOME-GIT] 🗑️  Uninstall awesome-git"
    cmds:
      - task: uninstall:linux
        platforms: [linux]
      - task: uninstall:windows
        platforms: [windows]

  update:
    desc: "[AWESOME-GIT] 🔄 Update awesome-git to latest version"
    cmds:
      - task: update:linux
        platforms: [linux]
      - task: update:windows
        platforms: [windows]

  verify:
    desc: "[AWESOME-GIT] ✅ Verify awesome-git installation"
    cmds:
      - |
        if ! command -v awesome-git >/dev/null 2>&1; then
          task -t lib.yaml messaging:error MESSAGE="awesome-git not found in PATH"
          exit 1
        fi
        {{ .gum_ok }} "✅ awesome-git found: $(command -v awesome-git)"
        version=$(awesome-git --version 2>&1 || echo "unknown")
        {{ .gum_info }} "📋 Version: $version"
        {{ .gum_ok }} "✅ awesome-git is functional"

  install:linux:unzip:
    platforms: [linux]
    internal: true
    cmds:
      - task: lib:linux-pkg:install
        vars:
          PACKAGE: unzip

  install:linux:
    platforms: [linux]
    internal: true
    deps: [install:linux:unzip]
    cmds:
      - task: lib:github-release:install:zip
        vars:
          TOOL_NAME: awesome-git
          REPO: NeonTowel/awesome-git
          VERSION: "{{ .AWESOME_GIT_VERSION }}"
          ARCHIVE_NAME: "awesome-git-linux.zip"
          BINARY_PATH: "awesome-git/awesome-git"

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: lib:scoop:neontowel-install
        vars:
          APP: awesome-git

  uninstall:linux:
    internal: true
    platforms: [linux]
    cmds:
      - rm -f $HOME/bin/awesome-git
      - task: lib:messaging:success
        vars:
          MESSAGE: "awesome-git uninstalled successfully"

  uninstall:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: lib:scoop:uninstall
        vars:
          APP: awesome-git

  update:linux:
    internal: true
    platforms: [linux]
    cmds:
      - task: install:linux

  update:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: lib:scoop:update
        vars:
          APP: awesome-git
