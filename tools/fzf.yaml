version: '3'

includes:
  LIB: ../lib.yaml
  TOOL: ../models/tool.yaml

tasks:
  default:
    - task: TOOL:default

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: LIB:scoop:install
        vars:
          APP: fzf

  install:
    desc: "[FZF] ✨ Install fzf"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
