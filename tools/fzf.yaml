version: '3'

tasks:
  default:
    - task --list --taskfile tools/fzf.yaml

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::scoop:install
        vars:
          APP: fzf

  install:
    desc: "[FZF] ✨ Install fzf"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
