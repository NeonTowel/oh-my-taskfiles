version: '3'

tasks:
  default:
    - task --list --taskfile tools/btop.yaml

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::scoop:install
        vars:
          APP: btop

  install:
    desc: "[BTOP] ✨ Install btop"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
