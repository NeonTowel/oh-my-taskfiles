version: '3'

includes:
  lib: ../lib.yaml
  tool: ../models/tool.yaml

tasks:
  default:
    - task: tool:default

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: lib:scoop:install
        vars:
          APP: btop

  install:
    desc: "[BTOP] ✨ Install btop"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
