# Default features to enable when setting up our Awesome Windows Experience
#
# Override these with ~/.config/omt/awesome-windows.env, which takes precedence.
# You can override one or all of the features (TOOLS, APPS, RUN). Just comment those that
# you don't want to override.

# Comma separated list of tools to install and configure, or '*' to install opinionated set of tools.
TOOLS=*

# Comma separated list of apps to install and configure, or '*' to install opinionated set of apps.
APPS=*

# Comma separated list of configurations to run, or '*' to run opinionated set of configurations.
RUN=*
