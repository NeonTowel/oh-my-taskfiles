version: '3'

tasks:
  install-success:
    required:
      vars: [ APP ]
    cmds:
      - |
        if [ "{{ .UNATTENDED }}" != "true" ]; then
          {{ .gum_ok }} " ✅ {{ .APP }} has been installed successfully."
        fi