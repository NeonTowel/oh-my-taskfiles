version: '3'

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml

tasks:
  default:
    - task: app:default

  install:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: lib:winget:install
        vars:
          APP: "CodeSector.TeraCopy"

  install:
    desc: "[TERACOPY] ✨ Install TeraCopy"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
