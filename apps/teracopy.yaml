version: '3'

tasks:
  default:
    - task --list --taskfile apps/teracopy.yaml

  install:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: ::winget:install
        vars:
          APP: "CodeSector.TeraCopy"

  install:
    desc: "[TERACOPY] ✨ Install TeraCopy"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
