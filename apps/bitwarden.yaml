version: '3'

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml

tasks:
  default:
    - task: app:default

  install:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: lib:winget:install
        vars:
          APP: "Bitwarden.Bitwarden"

  install:
    desc: "[BITWARDEN] ✨ Install Bitwarden"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
