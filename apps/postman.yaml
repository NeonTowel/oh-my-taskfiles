version: '3'

tasks:
  default:
    - task --list --taskfile apps/postman.yaml

  install:windows:
    platforms: [windows]
    internal: true
    cmds:
      - task: ::scoop:install
        vars:
          APP: postman

  install:
    vars:
      APP_NAME: postman
    desc: "[{{ .APP_NAME | upper }}] ✨ Install {{ .APP_NAME | title }}"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]

