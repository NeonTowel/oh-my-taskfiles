version: '3'

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml

tasks:
  install:windows:
    platforms: [windows]
    internal: true
    cmds:
      - task: lib:scoop:install
        vars:
          APP: postman

  install:
    vars:
      APP_NAME: postman
    desc: "[{{ .APP_NAME | upper }}] ✨ Install {{ .APP_NAME | title }}"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]

