version: '3'

tasks:
  install:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: ::winget:install
        vars:
          APP: "Proton.ProtonMail"

  install:
    desc: "[PROTONMAIL] ✨ Install Proton Mail"
    cmds:
      - task: install:windows
