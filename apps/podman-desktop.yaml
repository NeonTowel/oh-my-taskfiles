version: '3'

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml

tasks:
  default:
    - task: app:default

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: lib:scoop:extras-install
        vars:
          APP: podman-desktop

  install:
    desc: "[PODMAN-DESKTOP] ✨ Install Podman Desktop"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
