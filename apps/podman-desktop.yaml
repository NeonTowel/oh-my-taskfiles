version: '3'

tasks:
  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::scoop:extras-install
        vars:
          APP: podman-desktop

  install:
    desc: "[PODMAN-DESKTOP] ✨ Install Podman Desktop"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]
