version: '3'

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml

tasks:
  default:
    - task: app:default

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: lib:scoop:install
        vars:
          APP: translucenttb

  install:
    desc: "[TRANSLUCENTTB] ✨ Install TranslucentTB"
    cmds:
      - task: install:windows
        platforms: [windows]
