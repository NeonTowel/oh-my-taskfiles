version: '3'

vars:
  APP: "Anysphere.Cursor"
  NAME: "Cursor"

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml
  winget:
    taskfile: ../models/winget.yaml
    flatten: true

tasks:
  default:
    - task: app:default

  configure:
    desc: "[CURSOR] ⚙️  Configure Cursor (extensions + settings)"
    cmds:
      - task: lib:ide:configure:cursor
