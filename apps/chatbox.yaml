version: '3'

tasks:
  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::scoop:neontowel-install
        vars:
          APP: chatbox

  install:
    desc: "[CHATBOX] ✨ Install Chatbox AI chat bot"
    silent: true
    cmds:
      - task: install:windows
