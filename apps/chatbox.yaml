version: '3'

tasks:
  default:
    - task --list --taskfile apps/chatbox.yaml

  install:windows:
    platforms: [windows]
    internal: true
    silent: true
    cmds:
      - task: ::winget:install
        vars:
          APP: "Bin-Huang.Chatbox"

  install:
    desc: "[CHATBOX] ✨ Install Chatbox AI chat bot"
    silent: true
    cmds:
      - task: install:windows
