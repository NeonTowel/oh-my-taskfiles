version: '3'

tasks:
  install:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: ::winget:install
        vars:
          APP: "Proton.ProtonDrive"

  install:
    desc: "[PROTONDRIVE] ✨ Install Proton Drive"
    cmds:
      - task: install:windows
