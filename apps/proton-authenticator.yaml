version: '3'

tasks:
  install:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: ::winget:install
        vars:
          APP: "Proton.ProtonAuthenticator"

  install:
    desc: "[PROTONAUTHENTICATOR] ✨ Install Proton Authenticator"
    cmds:
      - task: install:windows
