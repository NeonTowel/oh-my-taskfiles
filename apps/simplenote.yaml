version: '3'

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml

tasks:
  default:
    - task: app:default

  install:windows:
    platforms: [windows]
    internal: true
    cmds:
      - task: lib:scoop:install
        vars:
          APP: simplenote

  install:
    vars:
      APP_NAME: simplenote
    desc: "[{{ .APP_NAME | upper }}] ✨ Install {{ .APP_NAME | title }}"
    silent: true
    cmds:
      - task: install:windows
        platforms: [windows]

