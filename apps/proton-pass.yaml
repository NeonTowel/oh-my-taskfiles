version: '3'

includes: 
  lib: ../lib.yaml
  app: ../models/app.yaml

tasks:
  default:
    - task: app:default

  install:windows:
    internal: true
    platforms: [windows]
    cmds:
      - task: lib:winget:install
        vars:
          APP: "Proton.ProtonPass"

  install:
    desc: "[PROTONPASS] ✨ Install Proton Pass"
    cmds:
      - task: install:windows
